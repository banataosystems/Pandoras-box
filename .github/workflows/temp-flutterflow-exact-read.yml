name: Temporary FlutterFlow Exact Widget Write

on:
  pull_request:
    branches:
      - feature/flutterflow-pandora-mobile-v1
    paths:
      - .github/flutterflow-read-trigger.txt

permissions:
  contents: read

jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - name: Test exact FlutterFlow widget partition write
        run: |
          set -euo pipefail
          curl --silent --show-error --get \
            'https://ivmvufhcsezyhczzondn.supabase.co/functions/v1/flutterflow-leaf-write-diagnostic' \
            --data-urlencode 'code=APAmC1D5W3B8ZZu68Tea5XnBBsXs4_HXfxerweXrccU'
