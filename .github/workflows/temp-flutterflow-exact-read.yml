name: Temporary FlutterFlow Context Locator

on:
  pull_request:
    branches:
      - feature/flutterflow-pandora-mobile-v1
    paths:
      - .github/flutterflow-read-trigger.txt

permissions:
  contents: read

jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - name: Locate FlutterFlow text widget from page context
        run: |
          set -euo pipefail
          curl --silent --show-error --get \
            'https://ivmvufhcsezyhczzondn.supabase.co/functions/v1/flutterflow-context-locator' \
            --data-urlencode 'code=APAmC1D5W3B8ZZu68Tea5XnBBsXs4_HXfxerweXrccU'
