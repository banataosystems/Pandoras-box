name: Temporary FlutterFlow Write Diagnostic

on:
  pull_request:
    branches:
      - feature/flutterflow-pandora-mobile-v1
    paths:
      - .github/flutterflow-read-trigger.txt

permissions:
  contents: read

jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - name: Diagnose exact FlutterFlow write behavior
        run: |
          set -euo pipefail
          curl --silent --show-error --get \
            'https://ivmvufhcsezyhczzondn.supabase.co/functions/v1/flutterflow-write-diagnostic' \
            --data-urlencode 'code=liFd_f2qELhrVeCaczF9p7iOkBczrp2r2WMsr7_GwmY'
