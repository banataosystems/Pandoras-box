name: Temporary FlutterFlow Exact Project Read

on:
  pull_request:
    branches:
      - feature/flutterflow-pandora-mobile-v1
    paths:
      - .github/flutterflow-read-trigger.txt

permissions:
  contents: read

jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - name: Read sanitized FlutterFlow project manifest
        run: |
          curl --fail --silent --show-error \
            'https://ivmvufhcsezyhczzondn.supabase.co/functions/v1/flutterflow-exact-project-read?code=XdwZFtX00C7GclUy1bKaop_LCUN8C4Vrzirpou9DUBY'
