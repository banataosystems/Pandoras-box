name: Temporary FlutterFlow Plain Language Verify

on:
  pull_request:
    branches:
      - feature/flutterflow-pandora-mobile-v1
    paths:
      - .github/flutterflow-read-trigger.txt

permissions:
  contents: read

jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - name: Verify saved FlutterFlow wording
        run: |
          set -euo pipefail
          curl --silent --show-error --get \
            'https://ivmvufhcsezyhczzondn.supabase.co/functions/v1/flutterflow-plain-language-verify' \
            --data-urlencode 'code=pgQ7TPl3ar8oOT9FcjVNhB2tra5qf96xJqmRnVSWZxY'
