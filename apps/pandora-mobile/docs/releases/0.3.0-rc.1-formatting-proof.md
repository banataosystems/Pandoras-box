# Pandora Mobile 0.3.0 RC1 — Formatting proof

**Evidence class:** exact-source formatting remediation  
**Production release:** false

## Triggering failure

GitHub Actions run `32196429046` on source `a932e0a5c03f3e671001235e7241d89874a37cda` passed exact checkout, brand verification, transparent-mark verification, dependency resolution, and source guards, then failed closed at `dart format --output=none --set-exit-if-changed`.

## Bounded diagnosis

Read-only formatter evidence run `32196426931` produced artifact `9346008263` and identified exactly one source file requiring pinned Dart formatting:

`apps/pandora-mobile/lib/features/approvals/approvals_screen.dart`

No production deployment or release promotion was performed.

## Remediation

A one-shot workflow used the repository-pinned Flutter 3.47.0 toolchain to format only the identified file. The workflow verified that no other source path changed, committed the formatted result, removed itself, and preserved the failed source and workflow evidence in Git history.

This evidence file is committed through the governed GitHub provider to trigger fresh pull-request CI on the resulting candidate. Passing CI is required before this candidate may advance to independent exact-head review. Physical Android verification and production promotion remain separate gates.
